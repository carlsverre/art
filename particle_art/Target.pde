class Target {
  PVector point;
  PVector direction;
  float distance;

  Target(PVector point, PVector direction, float distance) {
    this.point = point;
    this.direction = direction;
    this.distance = distance;
  }
}
